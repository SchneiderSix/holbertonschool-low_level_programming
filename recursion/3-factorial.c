#include "main.h"

/**
 * factorial - print factorial of num
 * @n: num
 * Return: n fac or -1
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else
	{
		return (n*factorial(n - 1);
	}
}
