#include "main.h"
#include <stdio.h>

/**
 * main - print nat num
 * until 98
 * @n: var int
 * Return:  return result
 */

int main()
{
	int numb, count;

	for(numb = 0; numb <= 98; numb++)
	{
		printf("%d, ", numb);
	}
	return (0);
}
