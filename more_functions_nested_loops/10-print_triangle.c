#include "main.h"

/**
 * print_square - print numbers
 * @size: int var
 * Return: Always 0.
 */

void print_triangle(int size)
{
	if (size > 0)
	{
		int i, n;
		
		for (i = 1; i > 0; i--)
		{
			for (n = size; n < i; j++)
			{
				_putchar(#);
			}
	}
	else
	{
		_putchar('\n');
	}
}
