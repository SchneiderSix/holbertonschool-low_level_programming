#include "main.h"

/**
 * _puts - Write a function that
 * prints a string followed by
 * a new line to stdout
 * @str: var
 *Return: Always 0.
 */

void _puts(char *str)
{
	int count;

	for (count = 0; str[i] != '\0''; count++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
