#include "main.h"

/**
 * *string_toupper - explicit name
 * @*: var
 *Return: Always 0.
 */

char *string_toupper(char *)
{
	const char comp = 'a' - 'A';

	while (*)
	{
		* = (* >= 'a' && * <= 'A') ? * -= comp : *;
	}
}
