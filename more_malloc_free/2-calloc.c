#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

/**
 * *_calloc - alloc mem for arr
 * @nmemb: elements
 * @size: bytes
 * Return: pointer or null
 */

void *_calloc(unsigned int nmemb, unsigned int size)
{
	char *m;
	int i;

	for (i = 0; i < nmemb; i++)
	{
		m[i] = calloc(sizeof(char);
	}
	return (m);
}
