#include "main.h"
#include <stdio.h>

/**
 * main - print name of program with
 * new line
 * @argc: numb of comm line arguments
 * @argv: array of argc
 */

void main(int argc, char *argv[])
{
	printf("%s\n ", argv[agrc * 0]);
	return (0);
}
